# =======================================================
# EduCatalog V3 — Backend Python Dependencies
# -------------------------------------------------------
# Python 3.12 — ห้ามใช้เวอร์ชันต่ำกว่า
# pin version ทุกตัวเพื่อ reproducible build (CI/CD)
# =======================================================


# -------------------------------------------------------
# Web framework
# -------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12


# -------------------------------------------------------
# Validation & Config (กฎ A5 — อ่าน .env ผ่าน settings)
# -------------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.6.1
email-validator==2.2.0


# -------------------------------------------------------
# Database — PostgreSQL 16 + SQLAlchemy 2.0 async
# -------------------------------------------------------
sqlalchemy[asyncio]==2.0.35
alembic==1.13.3
asyncpg==0.30.0


# -------------------------------------------------------
# Cache + Queue — Redis 7 + Celery
# -------------------------------------------------------
redis[hiredis]==5.1.1
celery==5.4.0
flower==2.0.1


# -------------------------------------------------------
# Object Storage — MinIO (S3 compatible)
# -------------------------------------------------------
minio==7.2.10


# -------------------------------------------------------
# Search — Elasticsearch 8 async client
# -------------------------------------------------------
elasticsearch[async]==8.15.1


# -------------------------------------------------------
# Security
# -------------------------------------------------------
# JWT sign/verify (HS256 — กฎ D2)
python-jose[cryptography]==3.3.0
# bcrypt password hashing (กฎ D3)
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
# Fernet encryption สำหรับ PII (กฎ D7)
cryptography==43.0.3


# -------------------------------------------------------
# External services
# -------------------------------------------------------
# Email — Resend
resend==2.4.0
# async HTTP client (CKAN sync, webhooks)
httpx==0.27.2


# -------------------------------------------------------
# Thai NLP — Search tokenizer (กลุ่ม 5)
# หมายเหตุ: ชื่อจริงคือ pythainlp (ไม่ใช่ pythainipl)
# -------------------------------------------------------
pythainlp==5.0.4


# -------------------------------------------------------
# Logging — JSON format (กฎ H4, H6)
# -------------------------------------------------------
structlog==24.4.0
python-json-logger==2.0.7


# -------------------------------------------------------
# Testing (กฎ E1, E2 — coverage ≥ 70%)
# -------------------------------------------------------
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
